@model List<SpeakerViewModel>
<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Speakers</h4>
                <a asp-controller="speaker" asp-action="create" class="btn btn-primary">Create speaker</a>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Image</th>
                                <th>Name</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td><img src="@item.ImageUrl" width="100" height="100"/></td>
                                    <td>@item.Name</td>
                                    <td>
                                        <a asp-controller="speaker" asp-action="details" asp-route-id="@item.Id">Details</a>
                                    </td>
                                    <td>
                                        <a asp-controller="speaker" asp-action="update" asp-route-id="@item.Id">Update</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>